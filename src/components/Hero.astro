---
import type { HeroContent } from '../types/site';
import BackendFlow from './BackendFlow.astro';

interface Props {
	content: HeroContent;
}

const { content } = Astro.props;
const [github, linkedIn] = content.socialLinks;
---

<section class="hero" aria-labelledby="hero-title">
	<div class="hero__inner container">
		<div class="hero__copy">
			<p class="hero__eyebrow">{content.eyebrow}</p>
			<h1 id="hero-title">{content.title}</h1>
			<p class="hero__stack">{content.stack}</p>
			<p class="hero__description">{content.description}</p>

			<div class="hero__actions" aria-label="Enlaces destacados">
				<a class="button button--primary" href={content.projectsHref}>{content.projectsLabel}</a>
				{github?.href ? (
					<a class="button button--secondary" href={github.href} rel="me noreferrer">{github.label}</a>
				) : (
					<span class="button button--secondary button--pending" aria-disabled="true">
						{github?.label}<span class="sr-only">, enlace pendiente</span>
					</span>
				)}
				{linkedIn?.href ? (
					<a class="hero__text-link" href={linkedIn.href} rel="me noreferrer">{linkedIn.label}</a>
				) : (
					<span class="hero__text-link hero__text-link--pending" aria-disabled="true">
						{linkedIn?.label}<span class="sr-only">, enlace pendiente</span>
					</span>
				)}
			</div>

			<ul class="hero__metadata" aria-label="Información profesional">
				<li>{content.location}</li>
				<li>{content.availability}</li>
			</ul>
		</div>

		<div class="hero__visual">
			<BackendFlow />
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		isolation: isolate;
		overflow: hidden;
	}

	.hero::before {
		position: absolute;
		z-index: -1;
		top: -18rem;
		left: -16rem;
		width: 40rem;
		height: 40rem;
		border: 1px solid rgb(var(--color-hero-decoration-rgb) / 18%);
		border-radius: 50%;
		box-shadow: 0 0 0 8rem rgb(var(--color-hero-decoration-rgb) / 10%), 0 0 0 16rem rgb(var(--color-hero-decoration-rgb) / 7%);
		content: '';
	}

	.hero__inner {
		display: grid;
		grid-template-columns: minmax(0, 1.03fr) minmax(22rem, 0.8fr);
		align-items: center;
		gap: clamp(3rem, 8vw, 7rem);
		padding-block: clamp(4.5rem, 11vh, 7.5rem) clamp(5.5rem, 13vh, 9rem);
	}

	.hero__copy {
		max-width: 42rem;
	}

	.hero__eyebrow {
		display: flex;
		align-items: center;
		gap: var(--space-3);
		margin: 0 0 var(--space-4);
		color: var(--color-text-muted);
		font-family: var(--font-mono);
		font-size: 0.72rem;
		letter-spacing: 0.13em;
		text-transform: uppercase;
	}

	.hero__eyebrow::before {
		width: 1.75rem;
		height: 1px;
		background: var(--color-border-strong);
		content: '';
	}

	h1 {
		max-width: 12ch;
		margin: 0;
		font-size: clamp(2.85rem, 6.2vw, 5.75rem);
		font-weight: 540;
		letter-spacing: -0.065em;
		line-height: 0.96;
		text-wrap: balance;
	}

	.hero__stack {
		margin: var(--space-6) 0 0;
		color: var(--color-text-muted);
		font-family: var(--font-mono);
		font-size: clamp(0.72rem, 1.2vw, 0.86rem);
		letter-spacing: 0.045em;
	}

	.hero__description {
		max-width: 35rem;
		margin: var(--space-8) 0 0;
		color: #c5c5c2;
		font-size: clamp(1.05rem, 1.5vw, 1.25rem);
		line-height: 1.55;
	}

	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var(--space-3);
		margin-top: var(--space-8);
	}

	.button {
		display: inline-flex;
		min-height: 2.9rem;
		align-items: center;
		justify-content: center;
		padding: 0.72rem 1.1rem;
		border: 1px solid var(--color-border-strong);
		border-radius: var(--radius-sm);
		font-family: var(--font-mono);
		font-size: 0.72rem;
		letter-spacing: 0.04em;
		text-decoration: none;
		transition: background var(--duration-base) var(--ease-out), color var(--duration-base) var(--ease-out), border-color var(--duration-base) var(--ease-out), transform var(--duration-base) var(--ease-out);
	}

	.button--primary {
		border-color: var(--color-text);
		background: var(--color-text);
		color: var(--color-canvas);
	}

	.button--primary:hover,
	.button--primary:focus-visible {
		transform: translateY(-2px);
		background: transparent;
		color: var(--color-text);
	}

	.button--secondary:hover,
	.button--secondary:focus-visible {
		border-color: var(--color-text);
		background: var(--color-surface-raised);
	}

	.button--pending,
	.hero__text-link--pending {
		cursor: not-allowed;
		opacity: 0.45;
	}

	.hero__text-link {
		padding: 0.65rem 0.4rem;
		color: var(--color-text-muted);
		font-family: var(--font-mono);
		font-size: 0.72rem;
		text-underline-offset: 0.28rem;
	}

	.hero__text-link:hover,
	.hero__text-link:focus-visible {
		color: var(--color-text);
	}

	.hero__metadata {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-3) var(--space-8);
		padding: var(--space-8) 0 0;
		margin: var(--space-8) 0 0;
		border-top: 1px solid var(--color-border);
		color: var(--color-text-muted);
		font-family: var(--font-mono);
		font-size: 0.67rem;
		letter-spacing: 0.045em;
		list-style: none;
	}

	.hero__metadata li {
		display: flex;
		align-items: center;
		gap: var(--space-2);
	}

	.hero__metadata li::before {
		width: 0.35rem;
		height: 0.35rem;
		border: 1px solid var(--color-text-muted);
		border-radius: 50%;
		content: '';
	}

	.hero__metadata li:last-child::before {
		background: var(--color-text-muted);
	}

	@media (max-width: 58rem) {
		.hero__inner {
			grid-template-columns: 1fr;
			gap: var(--space-16);
			padding-block: clamp(3.75rem, 10vw, 6rem) clamp(5rem, 14vw, 8rem);
		}

		.hero__copy {
			max-width: 46rem;
		}

		h1 {
			max-width: 13ch;
		}

		.hero__visual {
			width: min(100%, 37rem);
		}
	}

	@media (max-width: 34rem) {
		h1 {
			font-size: clamp(2.5rem, 13vw, 3.9rem);
		}

		.hero__actions {
			align-items: stretch;
		}

		.button {
			flex: 1 1 calc(50% - var(--space-3));
		}

		.hero__text-link {
			flex-basis: 100%;
			padding-left: 0;
		}
	}
</style>
