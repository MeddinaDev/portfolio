<figure class="backend-flow" aria-labelledby="flow-title" aria-describedby="flow-description">
	<figcaption class="backend-flow__heading">
		<span id="flow-title">Backend request flow</span>
		<span aria-hidden="true">01 — 06</span>
	</figcaption>

	<p id="flow-description" class="sr-only">
		Una petición HTTP atraviesa la autenticación JWT, el controlador, el servicio y
		PostgreSQL antes de devolver una respuesta 200 OK.
	</p>

	<ol class="flow" aria-label="Recorrido de una petición al backend">
		<li class="flow__step flow__step--request" style="--step: 0">
			<span class="flow__index">01</span>
			<span class="flow__content">
				<span class="flow__label">HTTP Request</span>
				<code>POST /api/v1/...</code>
			</span>
		</li>
		<li class="flow__step" style="--step: 1">
			<span class="flow__index">02</span>
			<span class="flow__content">
				<span class="flow__label">JWT Auth</span>
				<small>Token validation</small>
			</span>
		</li>
		<li class="flow__step" style="--step: 2">
			<span class="flow__index">03</span>
			<span class="flow__content">
				<span class="flow__label">Controller</span>
				<small>Request mapping</small>
			</span>
		</li>
		<li class="flow__step" style="--step: 3">
			<span class="flow__index">04</span>
			<span class="flow__content">
				<span class="flow__label">Service</span>
				<small>Business logic</small>
			</span>
		</li>
		<li class="flow__step" style="--step: 4">
			<span class="flow__index">05</span>
			<span class="flow__content">
				<span class="flow__label">PostgreSQL</span>
				<small>Persistence</small>
			</span>
		</li>
		<li class="flow__step flow__step--response" style="--step: 5">
			<span class="flow__index">06</span>
			<span class="flow__content">
				<span class="flow__label">200 OK</span>
				<small>Response</small>
			</span>
		</li>
	</ol>
</figure>

<style>
	.backend-flow {
		position: relative;
		padding: clamp(1.1rem, 3vw, 1.6rem);
		margin: 0;
		overflow: hidden;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background:
			linear-gradient(var(--color-border) 1px, transparent 1px),
			linear-gradient(90deg, var(--color-border) 1px, transparent 1px),
			var(--color-surface);
		background-size: 2.5rem 2.5rem;
		box-shadow: var(--shadow-soft);
	}

	.backend-flow::before {
		position: absolute;
		inset: 0;
		background: radial-gradient(circle at 75% 22%, rgb(255 255 255 / 6%), transparent 42%);
		content: '';
		pointer-events: none;
	}

	.backend-flow__heading {
		position: relative;
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding-bottom: var(--space-4);
		border-bottom: 1px solid var(--color-border);
		color: var(--color-text-muted);
		font-family: var(--font-mono);
		font-size: 0.67rem;
		letter-spacing: 0.11em;
		text-transform: uppercase;
	}

	.flow {
		position: relative;
		display: grid;
		gap: 0.55rem;
		padding: var(--space-6) 0 0;
		margin: 0;
		list-style: none;
	}

	.flow__step {
		position: relative;
		display: grid;
		grid-template-columns: 2rem 1fr;
		min-height: 3.35rem;
		align-items: center;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-sm);
		background: var(--color-surface-step);
		color: var(--color-text-muted);
		animation: node-signal 10s linear infinite;
		animation-delay: calc(var(--step) * 1.35s);
	}

	.flow__step:not(:last-child)::after {
		position: absolute;
		z-index: 2;
		bottom: -0.64rem;
		left: 50%;
		width: 0.3rem;
		height: 0.3rem;
		transform: translate(-50%, -0.1rem) scale(0.6);
		border-radius: 50%;
		background: var(--color-text);
		box-shadow: 0 0 0.7rem rgb(255 255 255 / 70%);
		content: '';
		opacity: 0;
		animation: connector-pulse 10s linear infinite;
		animation-delay: calc(var(--step) * 1.35s + 0.65s);
	}

	.flow__index {
		display: grid;
		height: 100%;
		place-items: center;
		border-right: 1px solid var(--color-border);
		font-family: var(--font-mono);
		font-size: 0.6rem;
	}

	.flow__content {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: var(--space-4);
		padding: 0.7rem 0.85rem;
	}

	.flow__label,
	.flow code,
	.flow small {
		font-family: var(--font-mono);
	}

	.flow__label {
		color: var(--color-text);
		font-size: clamp(0.72rem, 1.4vw, 0.83rem);
		letter-spacing: 0.045em;
		text-transform: uppercase;
	}

	.flow code,
	.flow small {
		color: inherit;
		font-size: 0.62rem;
		letter-spacing: 0.02em;
	}

	.flow__step--response {
		background: var(--color-text);
		color: #555;
		animation-name: response-signal;
	}

	.flow__step--response .flow__label {
		color: var(--color-canvas);
	}

	@keyframes node-signal {
		0%, 13% {
			border-color: rgb(255 255 255 / 46%);
			background-color: var(--color-surface-raised);
			box-shadow: 0 0 1.5rem rgb(255 255 255 / 7%);
			color: var(--color-text);
		}
		20%, 100% {
			border-color: var(--color-border);
			background-color: var(--color-surface-step);
			box-shadow: none;
			color: var(--color-text-muted);
		}
	}

	@keyframes connector-pulse {
		0% {
			transform: translate(-50%, -0.2rem) scale(0.6);
			opacity: 0;
		}
		4%, 9% {
			opacity: 1;
		}
		13% {
			transform: translate(-50%, 0.55rem) scale(1);
			opacity: 0;
		}
		14%, 100% {
			opacity: 0;
		}
	}

	@keyframes response-signal {
		0%, 13% {
			border-color: #fff;
			background-color: #fff;
			box-shadow: 0 0 1.5rem rgb(255 255 255 / 14%);
			color: #222;
		}
		20%, 100% {
			border-color: var(--color-text);
			background-color: var(--color-text);
			box-shadow: none;
			color: #555;
		}
	}

	@media (max-width: 26rem) {
		.flow__content {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.1rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.flow__step,
		.flow__step::after {
			animation: none;
		}
	}
</style>
